'use strict'

const thumbnail = {
    generate: (file) => {
        // https://www.npmjs.com/package/video-thumbnail-generator

        /**
         * Need to change to store vids in file system.
         * Probably a better idea anyway. 
         */
    }
}

module.exports = thumbnail
